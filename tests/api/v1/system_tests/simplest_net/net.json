{
    "transitions": [
        {
            "inputs": ["start"],
            "action": {
                "type": "notify",
                "url": {{ webhook_url('simple/webhook') }}
            }
        }
    ],
    "initialMarking": ["start"]
}
