{
    "transitions": [
        {
            "inputs": ["start"],
            "action": {
                "type": "notify",
                "url": "http://localhost:{{ callback_port }}/simple/callback"
            }
        }
    ]
}
