{
    "entry_places": ["start"],
    "transitions": [
        {
            "inputs": ["start"],
            "action": {
                "type": "notify",
                "url": {{ callback_url('simple/callback') }}
            }
        }
    ]
}
